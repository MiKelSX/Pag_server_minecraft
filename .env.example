# Variables de entorno para desarrollo local
# Copia este archivo a .env en tu máquina local para desarrollo

# Puerto en el que correrá el servidor (Railway lo asigna automáticamente)
PORT=3000

# Ambiente
NODE_ENV=production

# URL de la base de datos JSON (no cambiar)
DATABASE_PATH=/app/basedatos_votos.json
